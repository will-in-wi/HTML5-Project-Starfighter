// No weapon; does nothing
class NoWeapon {
  // Does nothing. Used for ships such as freighters.

  fire(direction, currPoint) {
    // Again, does nothing.
  }

  nofire() {
    // Again, does nothing.
  }
}

export default NoWeapon;
